#pragma once

/// Used for storing particle data.
class VulkanStorageBuffer {
    public:
        /// Create new storage buffer.
        VulkanStorageBuffer();
        
        /// Destructor.
        ~VulkanStorageBuffer();
};
