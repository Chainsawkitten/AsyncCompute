#pragma once

/// Pipeline used to update particles.
class ComputePipeline {
    public:
        /// Create new compute pipeline.
        ComputePipeline();
        
        /// Destructor.
        ~ComputePipeline();
};
